import {combineReducers} from 'redux'

const flashcards = (state = [], action) => {
  // switch (action.type) {
  //   default:
  //     return state
  // }
  return state;
};

export default combineReducers({
  flashcards
})
